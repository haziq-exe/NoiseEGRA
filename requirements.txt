# Core generation + RMS calibration
torch
transformers
accelerate

# Coherence checks: the heuristics need nothing, the sentence-coherence check
# reuses the embedding model below, and the perplexity checks want a small causal
# LM (scripts/score_diversity.py --coherence-model Qwen/Qwen2.5-0.5B).

# Diversity / creativity metrics (Vendi score, Self-BLEU, embeddings)
# The default embedding model is Qwen/Qwen3-Embedding-0.6B; scripts/score_diversity.py
# --embedding-model bge-m3 switches back to what the published Arabic runs used.
sentence-transformers
vendi-score
numpy
pandas

# LLM-judge scoring scripts: scripts/score_stories_gpt52.py, scripts/extract_parts_vendi.py
openai
python-dotenv

# Hugging Face auth (used by the experiment notebook)
huggingface_hub

# Optional: spaCy PERSON NER for the EGRA name-based constraint checks
# (C14/C17/C18 are skipped unless a name extractor or spaCy model is provided),
# and for scripts/score_diversity.py --plot-backend spacy:
#   pip install spacy && python -m spacy download en_core_web_sm
# spacy

# NOTE: The newest Arabic models (e.g. Jais-2) may require transformers built
# from source rather than the latest PyPI release:
#   pip install git+https://github.com/huggingface/transformers.git
