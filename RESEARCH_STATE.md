# Where the research stands

Read this before running anything. `AGENTS.md` covers the mechanics (Kaggle,
tests, git rules); this covers what the project is trying to show, what is
already known, and the traps that have produced wrong numbers.

Companion documents:

- `METHODS_TRIED.md` — every condition ever run, what it did, what it scored.
  Generated by `scripts/build_method_registry.py`. **Check it before proposing a
  mechanism.** A suite was once written and nearly launched for an idea that had
  been run thirteen rounds earlier and was a null.
- `EXPERIMENT_LOG.md` — the narrative, round by round: what was asked, what came
  back, what it changed.

## The goal, stated precisely

A cheap inference-time method that **at the same time**

1. improves constraint compliance over the unmodified model, and
2. improves output diversity over the unmodified model,

and beats what you get for free by turning the sampling temperature up. Either
one alone is easy and neither counts on its own. The method must be an
extension of the original architecture — **constraint steering plus noise** —
not a replacement for it.

Diversity is the Vendi score: the effective number of distinct stories in a set.
Compliance is the mean number of requirements broken per story, out of fifteen.

## Current best result

100 stories per condition, Qwen3-1.7B, the thirteen monotone requirements,
diversity measured on the first 40 words of each story.

| condition | broken/13 | Vendi | any degeneracy |
|---|---|---|---|
| the model as it ships | 7.78 | 6.93 | 64% |
| constraint push at 3, alone | 3.94 | 5.67 | 54% |
| perturbation at 0.1, alone | 7.08 | 9.94 | 41% |
| **constraint push at 3 + perturbation at 0.1** | **3.72** | **8.26** | **33%** |

Scored on the thirteen requirements in force at the time. Two more were added
afterwards — no sentence written twice, no opening reused more than three times —
because the thirteen could be satisfied by collapsed text. Re-scored on all
fifteen the ordering holds, 4.61 broken against 8.60, and this is the arm that
loses one of the two new rules: on duplicate sentences the unmodified model passes
71% and the push passes 28%. The perturbation is what pulls that back to 38%.
"Any degeneracy" is the share of stories with a repeated three-gram above 25%, a
repeated five-gram above 15%, or an opening used four or more times.

The last row is the first arm in the project to beat the unmodified model on
compliance, diversity and degeneracy at once. Each half alone wins one axis and
loses or barely moves the other; together they win both. That is the result the
work rests on.

**Not yet established:** whether it beats the *tuned decoding* baseline. Raising
the temperature to 1.8 with top-k 40 gives 7.21 broken and Vendi 10.65, which
beats the default on both axes too — but that Vendi was measured without
truncation and is not comparable to the numbers above. `r17-headline` runs the
decoding grid and the method in one command under one truncation to settle it.

## The method

Two mechanisms applied to the residual stream at layers 10–18.

**The constraint push** supplies compliance. One direction per requirement,
extracted by contrastive mean difference from twelve length-matched minimal pairs
each (`noiseegra/data/steering_pairs_en.json`), made mutually orthogonal by
Löwdin, summed, and renormalised so the *total* push has a fixed length. The
renormalisation matters: without it, summing k directions at coefficient beta
gives a push of length beta·sqrt(k), so adding a requirement silently raises the
dose, and the number of requirements and the strength of the intervention are the
same knob.

**The perturbation** supplies diversity. One constant offset per story, drawn
from the subspace along which whole stories' activations differ from one another,
projected clear of the constraint directions so it cannot move a requirement
either way, and applied to the prompt positions only — the model is moved
somewhere else before it writes a token and then decodes unperturbed, so a large
shift costs no fluency.

Both are one vector added per layer. Inference cost is the same as generation.

## What is settled

**Requirements split by shape, and only one half can be steered.** A *monotone*
requirement — more present tense, simpler words, shorter sentences — has a
direction that agrees with it. A *banded* one — "between 50 and 65 words",
"exactly two quoted lines" — does not: a constant push has no notion of enough,
and sails through the target. Across thirty-six steered conditions, **none ever
improved a banded requirement over leaving the model alone.** Scaling the push by
the error measured on the decoded text so far reduces the damage about fivefold
and does not remove it. This is why the main comparison uses thirteen
requirements that are all one-sided (`MONOTONE_CONSTRAINTS`).

**Steering buys compliance and spends diversity; perturbation does the reverse.**
This is the whole reason both are needed. Steering alone: 7.78 → 3.94 broken,
Vendi 6.93 → 5.67. Perturbation alone: Vendi 6.93 → 9.94, broken 7.78 → 7.08.

**Holding the dose fixed holds the diversity fixed.** Turning the constraint
vector without changing its length moved Vendi by +0.12; re-weighting the
requirements at a fixed total push moved it by −0.43. Both nulls. Diversity only
ever arrives when perturbation energy is added off the constraint subspace. Treat
any claim of diversity-at-no-cost as wrong until it survives this.

**Choosing the perturbation set jointly does not help.** Laying the whole set of
per-story offsets out so they repel — same length, same subspace, same mean
similarity, fewer near-duplicates — was tested against independent draws in four
matched pairs and was slightly *worse* in all four (9.94→9.68, 12.71→11.94,
8.26→7.95, 9.56→8.81). Covering the offset subspace evenly does not cover the
output space evenly. Implemented as `offset_draw="spread"`; leave it off.

**The requirement set must be checked for what collapse does to it.** Thirteen of
the fifteen get *easier* as the writing falls apart — short sentences, short
words, a plain opening, no subordinate clauses. Any new requirement has to be
tested against a deliberately collapsed story, and
`tests/test_english_smoke.py` does that.

**Eight of nine directions control their own requirement**, verified one at a
time (`--suite directions`): present tense 20→100%, dialogue 3→100%, short
sentences 47→97%, a named character 13→80%, simple register 60→80%, sensory
50→77%, simple syntax 57→70%, varied openers 27→40%. The adverb direction
(`plain_words`) fails in both signs and degenerates the text; it is scored but
**not steered**. Pass `--steer-vectors` without it.

**The model as it ships is not a strong baseline.** At its defaults it loops on
32% of stories for this prompt, which inflates its broken count and holds its
Vendi down. Any comparison that only beats the default is weak.

## Traps that have already produced wrong numbers

**Degenerate text scores well on both metrics.** A set of differently-broken
stories really is mutually dissimilar, so looping raises Vendi, and collapsed
text trivially satisfies requirements like "at most two adverbs". A result was
once reported whose best arm contained *"I'm going to the store"* eight times.
Repetition is therefore one of the thirteen scored requirements, every table
prints a looping column, and **you read the stories before reporting a number**
(`scripts/read_stories.py`, or index into the state file).

**Vendi rises with length.** Steering shortens the stories — 47 words against the
baseline's 85 — so an untruncated comparison partly measures that. Always pass
`--truncate-words 40`, and never compare a Vendi measured under one truncation
with one measured under another.

**Qwen3 ships top_p and top_k in its generation config.** A condition that does
not set them inherits them, so the "plain" baseline is not untruncated sampling.
A top-p 0.95 arm at temperature 1.0 once came back identical to the baseline in
every digit across a hundred stories.

**Run ids must record every setting that differs.** Two arms whose ids collide
share a file and the second silently overwrites the first — a wrong answer with
no error. `tests/test_suites_build.py` builds every suite in the runner's
`--suite` choices and asserts distinct ids; it takes its list from the runner, so
a new suite cannot escape it.

**Sharded runs write one score file per GPU.** The merge used to let one
overwrite the other, so half the diversity numbers vanished and a missing row
looks exactly like a condition that was never run. Fixed; `scripts/read_monotone.py`
also recovers them from the live log.

**Numbers are comparable only within a round.** Each round used its own
requirement set, prompt and sometimes its own model. Never build a table across
rounds.

## Reading a run

    python scripts/read_monotone.py experiments/<name> --name <name> [--full]

Prints compliance, diversity, looping and length per condition, sorted, against
the unmodified model, and marks arms that beat it on both axes. Condition names
are written out as descriptions, not assembled from setting names.

## In flight

`r17-headline` — the decoding grid and the method's frontier in one command, on
the fifteen requirements, all scored at 40-word truncation. 18 conditions x 100
stories. Settles whether the method beats tuned decoding rather than only the
default. An earlier attempt was stopped twice: once because the diversity was not
length-matched, once because it was running on the thirteen-requirement set that
rewards collapse.

## Open questions

- Does the method beat the temperature curve, or only the default? (`r17`)
- The perturbation is applied at the prompt only. Applying it while writing was
  worse in earlier rounds, but has not been retried since the budget and the
  monotone set changed what steering does.
- Three mechanisms are implemented and have never run on a GPU: perturbation
  *not* projected clear of the constraints (the ablation that would show the
  projection earns its place), random directions instead of extracted ones (the
  control that would show the directions are not just noise), and activation-space
  feedback.
- Whether any representation-level intervention can improve a banded requirement
  at all. Present evidence says no.
